<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.health.mapper.CheckItemMapper">
<!--    查询所有-->
    <select id="findAll" resultType="com.health.response.Result">
       SELECT * FROM t_checkitem
    </select>

<!--    查询所有带分页-->
    <select id="search" resultType="com.health.pojo.CheckItem">
        select * from t_checkitem
        <if test="value != null and value.length > 0">
            where code = #{value} or name = #{value} or type=#{value}
        </if>
    </select>

<!--    根据id查询-->
    <select id="findOne" resultType="com.health.pojo.CheckItem" parameterType="int">
        select * from t_checkitem where id=#{id}
    </select>

<!--    添加-->
    <insert id="add" parameterType="com.health.pojo.CheckItem" keyProperty="id" useGeneratedKeys="true">
        insert into t_checkitem(code,name,sex,age,price,type,remark,attention)
                      values
        (#{code},#{name},#{sex},#{age},#{price},#{type},#{remark},#{attention})
    </insert>

<!--    修改-->
    <update id="edit" parameterType="com.health.pojo.CheckItem">
        update t_checkitem
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="sex != null">
                sex = #{sex},
            </if>
            <if test="code != null">
                code = #{code},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="attention != null">
                attention = #{attention},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
        </set>
        where id = #{id}
    </update>


<!--    删除-->
    <delete id="delete" parameterType="int">
         delete from t_checkitem where id = #{id}
    </delete>
</mapper>